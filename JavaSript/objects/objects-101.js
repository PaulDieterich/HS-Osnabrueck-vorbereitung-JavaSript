let myBook = {
    title: '1984',
    author: 'George Orwell',
    pageCount: 326
}
console.log(`${myBook.title} by ${myBook.author}`)

myBook.title = "Animal Farm"

console.log(`${myBook.title} by ${myBook.author}`)


//Callenge area

// name, age, location 
//Androw is 27 and lives in Philadelphia
//Increase age by 1 and print massage again 
let person = {
    name: "Paul", 
    age: 21, 
    city: "Bremen"
}
console.log(`${person.name} is ${person.age} and lives in ${person.city}`);

person.age++ 

console.log(`${person.name} is ${person.age} and lives in ${person.city}`);